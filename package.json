{
  "name": "services-health",
  "version": "1.0.0",
  "description": "Monitor de conectividade móvel (Claro, Vivo, TIM) - Ipiranga/PR",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "bun x @biomejs/biome check src/"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "types": "^0.1.1",
    "typescript": "^7.0.2"
  }
}